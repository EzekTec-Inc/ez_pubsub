Plan for /speckit-implement:
1. Create a checkpoint and confirm the current failing baseline.
2. Add/adjust tests to lock the sync core and define feature-gated async behavior.
3. Refactor the library back to a stable sync core and move async support behind a separate `async` feature/module.
4. Implement Tokio-first async handlers and channel subscriptions as an additive API.
5. Run fmt/check/test (with and without the async feature) and summarize results.